using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FixPositioning : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //change character's positioning
        //transform.position = new Vector3(-0.28f, 0.401f, -6.89f);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
